import { Container } from "@mui/material";
import React from "react";

export default function Setting() {
  return (
    <div>
      환경설정 페이지 입니다. 사용자 정보 수정이랑 같이,위치 정보 끄고 켜기,
      다크모드 설정, 쿠키 저장 기능 키고끄기 이런거 넣으면 좋을 것 같아서,
      컴포넌트 이름을 userSetting으로 안하고, setting으로 만들었습니다.
    </div>
  );
}
